Metadata:
  test: zzz
Resources:
  ContentBucket:
    Type: AWS::S3::Bucket
    Metadata:
      Test:
        Fn::Sub: bar-abc-${AWS::AccountId}-xyz
      TestObj:
        TestObject:
          A: b
    Properties:
      BucketName: bar
