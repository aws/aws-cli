{
  "type": "bugfix",
  "category": "dynamodb",
  "description": "``dynamodb scan`` and ``dynamodb query`` now sum ``ConsumedCapacity`` (including per-index capacity under ``GlobalSecondaryIndexes``/``LocalSecondaryIndexes``) across all pages when auto-pagination is enabled, instead of reporting only the final page (CLI-4199)."
}
