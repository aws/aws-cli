Parameters:
  Name:
    Type: String
Resources:
  Bucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Ref Name
Outputs:
  BucketArn:
    Value: !GetAtt Bucket.Arn
  BucketArnSub:
    Value: !Sub ${Bucket.Arn}
  BucketRef:
    Value: !Ref Bucket

