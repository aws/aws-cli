Parameters:
  Name:
    Type: String
    Default: TestBucket

Resources:
  Bucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Ref Name

Outputs:
  BucketArn:
    # Missing Value property - this should cause an error
    Description: The ARN of the bucket
