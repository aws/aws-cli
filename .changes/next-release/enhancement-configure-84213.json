{
  "type": "enhancement",
  "category": "configure",
  "description": "``aws configure agent-toolkit`` now defaults to the ``us-east-1`` region unless ``--region`` is given, and always runs against ``us-east-1`` when launched from the Agent Toolkit prompt. Outside the commercial partition the prompt is replaced by a non-interactive tip, so accepting it does not results in a cross-partition call."
}
