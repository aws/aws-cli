Parameters:
  Count:
    Type: Number
    Default: 1

ParameterSchema:
  Count:
    Type: Number
    Minimum: 1
    Maximum: 10

Resources:
  Bucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub "test-bucket-${Count}"
