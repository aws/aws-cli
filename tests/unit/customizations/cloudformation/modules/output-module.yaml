Inputs:
  Name:
    Type: String
Resources:
  Bucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Ref Name
References:
  BucketArn: !GetAtt Bucket.Arn
  BucketArnSub: !Sub ${Bucket.Arn}
  BucketRef: !Ref Bucket

