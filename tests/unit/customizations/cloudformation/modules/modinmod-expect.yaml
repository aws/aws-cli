Parameters:
  ParentVal:
    Type: String
  AppName:
    Type: String
Resources:
  MySubBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: mod-in-mod-bucket
      XName:
        Fn::Sub: ${ParentVal}-abc
      YName:
        Fn::Sub: ${AppName}-xyz
